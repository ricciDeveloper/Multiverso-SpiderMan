padrão BEM:
    -Bloco
    -Elemento
    -Identificador
Padrão para nomenclatura de classes e IDs para o CSS.


rem: propriedade relativa. 1rem = 16px, onde é escalável de forma responsiva.

aula:adicionando videos a página interna dos Filmes


fancybox: lib para galerias
arquivos estáticos: html, css. São arquivos que não rodam em servidores, o próprio navegador interpreta eles.


cdn: arquivos estáticos espalhados globalmente para que você possa acessá-lo da forma mais próxima

REGEXR: expressão regular, conseguimos encontrar padrões.